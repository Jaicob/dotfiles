# Unix
alias ll="ls -al"
alias ln="ln -v"
alias mkdir="mkdir -p"
alias e="$EDITOR"
alias v="$VISUAL"

# Bundler
alias b="bundle"

# Rails
alias migrate="rake db:migrate db:rollback && rake db:migrate db:test:prepare"
alias s="rspec"

#CLI Tools
alias tclock="tty-clock -c -t"
alias dc="docker-compose"
alias dm="docker-machine"
alias cdandroid= "cd /Users/Jaicob/Library/Android/sdk"
alias dshell="docker exec -it webapp_web_1 /bin/bash"

#Other
alias axon="cd ~/Development/Axon/web-app"

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'

# Include custom aliases
[[ -f ~/.aliases.local ]] && source ~/.aliases.local
